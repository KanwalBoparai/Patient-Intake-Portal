# Committed intentionally per the assignment submission instructions
# (one-click `pnpm && pnpm dev` evaluation). Replace the placeholders with
# real values before pushing — see README "Setup (one-time)".

# Supabase project (Settings -> API)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR-SERVICE-ROLE-KEY

# Vercel Blob store (Vercel dashboard -> Storage -> Blob)
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_YOUR-TOKEN
